<Window x:Class="LogisticApp.Views.DataViews.OrderView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LogisticApp.Views.DataViews"
        mc:Ignorable="d"
        Title="OrderView" Height="450" Width="700
        ">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Height" Value="50"/>
            <Setter Property="Margin" Value="10"/>
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="AliceBlue"/>
            <Setter Property="Height" Value="700"/>
            <Setter Property="FontSize" Value="25"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" VerticalAlignment="Bottom">
            
            <Button Content="Fetch Data"/>
            <Button Content="Create new Order"/>
            <Button Content="Export to CSV"/>
            <Button Content="Exit Program"/>
        </StackPanel>
        <StackPanel Grid.Column="1">
            <ListBox>
            
            </ListBox>
        </StackPanel>
    </Grid>
</Window>
